import { HighlightRepoDirective } from './highlight-repo.directive';

describe('HighlightRepoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightRepoDirective();
    expect(directive).toBeTruthy();
  });
});
